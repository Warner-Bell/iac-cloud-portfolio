name: Deploy Site

on: 
  workflow_dispatch
  
jobs:
  SiteBuild:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@master

        env:
          AWS_REGION: 'us-east-1'

      - name: Build Site
        run: bash ./bin/website-deploy